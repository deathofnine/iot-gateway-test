# 测试用例文档

## 1. RPC 功能测试

### 1.1 DHT11 温湿度读取测试

| 用例ID | TC_RPC_001 |
|--------|------------|
| 用例名称 | DHT11 温湿度读取 |
| 用例描述 | 验证 RPC 调用 dht11_read 方法能正确读取温湿度数据 |
| 前置条件 | RPC 服务器运行在 192.168.137.114:1234 |
| 测试步骤 | 1. 建立 RPC 连接<br>2. 调用 dht11_read 方法<br>3. 获取返回结果 |
| 预期结果 | 返回非空数据，温度值在 0-50°C 范围，湿度值在 0-100% 范围 |
| 测试类型 | 功能测试 |
| 优先级 | 高 |

### 1.2 LED 控制测试 - 开灯

| 用例ID | TC_RPC_002 |
|--------|------------|
| 用例名称 | LED 开灯控制 |
| 用例描述 | 验证 RPC 调用 led_control(1) 能正确开灯 |
| 前置条件 | RPC 服务器运行正常 |
| 测试步骤 | 1. 建立 RPC 连接<br>2. 调用 led_control(1)<br>3. 验证返回值 |
| 预期结果 | 返回值为 0，表示执行成功 |
| 测试类型 | 功能测试 |
| 优先级 | 高 |

### 1.3 LED 控制测试 - 关灯

| 用例ID | TC_RPC_003 |
|--------|------------|
| 用例名称 | LED 关灯控制 |
| 用例描述 | 验证 RPC 调用 led_control(0) 能正确关灯 |
| 前置条件 | RPC 服务器运行正常 |
| 测试步骤 | 1. 建立 RPC 连接<br>2. 调用 led_control(0)<br>3. 验证返回值 |
| 预期结果 | 返回值为 0，表示执行成功 |
| 测试类型 | 功能测试 |
| 优先级 | 高 |

### 1.4 LED 连续开关测试

| 用例ID | TC_RPC_004 |
|--------|------------|
| 用例名称 | LED 连续开关测试 |
| 用例描述 | 验证 LED 能连续多次正确响应开关指令 |
| 前置条件 | RPC 服务器运行正常 |
| 测试步骤 | 1. 建立 RPC 连接<br>2. 连续执行 3 次开-关操作<br>3. 每次验证返回值 |
| 预期结果 | 每次操作均返回 0，连续操作稳定 |
| 测试类型 | 稳定性测试 |
| 优先级 | 中 |

---

## 2. MQTT 集成测试

### 2.1 MQTT 云端开灯指令

| 用例ID | TC_MQTT_001 |
|--------|------------|
| 用例名称 | MQTT 云端下发开灯指令 |
| 用例描述 | 验证华为云平台能成功下发 LED_ON 指令到设备 |
| 前置条件 | MQTT 设备已连接到华为云平台 |
| 测试步骤 | 1. 设备订阅 command 主题<br>2. 云端下发 {"paras":{"status":"ON"}} 指令<br>3. 设备通过 RPC 执行开灯<br>4. 验证执行结果 |
| 预期结果 | 设备成功接收并执行开灯指令 |
| 测试类型 | 集成测试 |
| 优先级 | 高 |

### 2.2 MQTT 云端关灯指令

| 用例ID | TC_MQTT_002 |
|--------|------------|
| 用例名称 | MQTT 云端下发关灯指令 |
| 用例描述 | 验证华为云平台能成功下发 LED_OFF 指令到设备 |
| 前置条件 | MQTT 设备已连接到华为云平台 |
| 测试步骤 | 1. 设备订阅 command 主题<br>2. 云端下发 {"paras":{"status":"OFF"}} 指令<br>3. 设备通过 RPC 执行关灯<br>4. 验证执行结果 |
| 预期结果 | 设备成功接收并执行关灯指令 |
| 测试类型 | 集成测试 |
| 优先级 | 高 |

### 2.3 MQTT 连续控制指令

| 用例ID | TC_MQTT_003 |
|--------|------------|
| 用例名称 | MQTT 连续控制 LED |
| 用例描述 | 验证云端能连续多次下发指令控制 LED |
| 前置条件 | MQTT 连接稳定 |
| 测试步骤 | 1. 连续下发 3 轮开-关指令<br>2. 每轮间隔 0.5 秒<br>3. 验证每次指令执行成功 |
| 预期结果 | 3 轮指令均执行成功，设备响应正常 |
| 测试类型 | 稳定性测试 |
| 优先级 | 中 |

---

## 3. 边界值测试

### 3.1 温湿度边界值测试

| 用例ID | TC_BOUNDARY_001 |
|--------|-----------------|
| 用例名称 | DHT11 温湿度边界值测试 |
| 用例描述 | 验证 DHT11 在极端边界条件下的数据准确性 |
| 测试数据 | 温度: 0°C, 50°C（正常范围边界）<br>湿度: 0%, 100%（正常范围边界） |
| 预期结果 | 数据仍在合理范围内，不出现异常值 |
| 测试类型 | 边界值测试 |
| 优先级 | 低 |

---

## 4. 缺陷记录

（暂无缺陷）

---

## 5. 测试执行记录

| 执行日期 | 测试人员 | 测试用例数 | 通过数 | 失败数 | 备注 |
|----------|----------|------------|--------|--------|------|
| 2026-05-17 | - | 7 | - | - | 初次编写 |